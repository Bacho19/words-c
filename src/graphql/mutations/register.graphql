mutation Register($options: UsernameAuthInput!) {
  register(options: $options) {
    user {
      id
      username
      createdAt
      updatedAt
    }

    errors {
      field
      message
    }
  }
}
