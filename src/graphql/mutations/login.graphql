mutation Login($options: UsernameAuthInput!) {
  login(options: $options) {
    user {
      id
      username
      createdAt
      updatedAt
    }
    errors {
      field
      message
    }
  }
}
